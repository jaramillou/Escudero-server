




    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">


    <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
   <!-- <script src='https://api.mapbox.com/mapbox-gl-js/v1.11.1/mapbox-gl.js'></script> --> 
    <link href='https://api.mapbox.com/mapbox-gl-js/v1.11.1/mapbox-gl.css' rel='stylesheet' />
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
    <script src="https://code.jquery.com/jquery-2.1.3.min.js"></script> <!-- para el aviso de coockies -->
    <link rel="stylesheet" href="assets/css/muro.css">
  




  <script>

      function muro(){

                document.body.innerHTML=null                  
                 var url = '/muro';
             
                abrirUrlHeader(url)

    }

function amigos(){

                document.body.innerHTML=null                  
                 var url = '/amigos';
             
                abrirUrlHeader(url)

    }


function abrirUrlHeader(url) {
    var token1 = localStorage.getItem('token');
    if (token1 != null) { //este bloque realiza la petición si se envió token con su cabecera del authoritation: token

        let h = new Headers();
        h.append('authorization', `${token1}`);
        let req = new Request(url, {
            method: 'GET',
            mode: 'cors',
            headers: h
        });



       fetch(req)
            .then(resp => resp.text())
            .then(data => {
                 //document.body.innerHTML = data;
                document.write(data);
                document.close()////////////////////////////////////////////puestso sin probar para que deje de cargar
              
                

            });
          

}

}



    function bypass(url){

 
        const token = localStorage.getItem('token')
        if (token != null){

            document.body.innerHTML=null 
             abrirUrlHeader(url);
    }
    
}


 function cerrar(){
             
               localStorage.removeItem('token')      
                location.href="/"
         
    }

 

</script>

